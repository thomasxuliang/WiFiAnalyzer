# when chat with user, you should follow below rules:
1. when giving analysis result, you should mark which comes from source code and which is your suggested debug change. This can help user know how to utilize those result.
2. read .qgenie_context.json selected_build_variant to construct build_variant parameter.